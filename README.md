Test Google API
1. Install JSON Viewer Pro Extension, because it will be easier to understand the JSON file
2. Add .env file with GOOGLE_MAPS_API_KEY
3. Run this commands to check if it is working:
  npm init -y
  npm install express node-fetch dotenv cors
  node server.js
4. Go to localhost:3000/restaurants
